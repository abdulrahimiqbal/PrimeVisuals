# Verdict — signed Type-II packet first survival round

## `KILL THE PROPOSED MECHANISM / RETAIN THE CANONICAL THEOREM TARGET`

The mechanism fails two frozen gates.

1. **Zero/local-mode cancellation is incomplete.**  The periodic
   `a_z#` comparator has zero Fourier coefficient exactly, so subtracting its
   correlation cannot algebraically cancel the actual finite zero mode of
   `Lambda-1`.  The prime mean term is numerically small here but requires an
   estimate.  The literal `d_2-log` calibration is worse: its nonzero dyadic
   mean leaves an observed `~(2 gamma)^2 XH` term.  The correct primary-source
   divisor approximant is not `d_2-log`.

2. **The claimed packet advantage disappears after the relevant local
   subtraction.**  Raw `Lambda-1` shows a fitted H-exponent reduction of
   `0.68--0.88`, but `aa-a_11#a_11#` shows gains
   `-1.058,-0.500,-0.256`.  The `z=5,11,23,47` sensitivity audit is sign- and
   scale-unstable; all nine fits with `z>=11` are negative.

The finite balanced Vaughan box does survive its resonator control: its norm
ratio is only `0.135--0.185`, and no tested rational phase saturates the
projected operator.  That says coefficient-sensitive bounds are not ruled out.
It is not positive evidence for the proposed delta/Kuznetsov route because no
stable factor-H gain remains in the target residual.

The theorem

\[
\sum_h\tau_H(h)\sum_n W(n/X)
\{a(n)a(n+h)-a_z^\sharp(n)a_z^\sharp(n+h)\}
\ll_A X\log^{-A}X
\]

is **not disproved**.  Proving it on a polynomial H-range would still be a
field-level result and would address the ABAC secondary scale.  What is killed
is the present claim that exact local-mode subtraction plus sum-before-norm
already exposes a credible factor-H mechanism.

Reopen only after all four repairs are made:

1. isolate the weighted prime zero mode explicitly and prove its required bound;
2. use the actual `d_2#` divisor-polynomial approximant for calibration;
3. freeze `B`, `z=(log X)^B`, and endpoint smoothing before inspecting data;
4. exhibit an analytic balanced Type-II lemma whose H-exponent advantage survives local subtraction on two independent scale blocks.
