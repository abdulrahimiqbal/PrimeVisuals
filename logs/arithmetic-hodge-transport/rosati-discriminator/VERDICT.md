# Rosati--Hodge breakthrough attempt: verdict

Status: **ARITHMETIC ROSATI LEAD / P2 RECONSTRUCTED / P3 RESET AT COMPARISON GATE**.

The campaign made two exact advances in the program but did not produce a new
field-level theorem.

1. In degree two, the unique symmetric Frobenius--Verschiebung adjoint form is
   `J=[[2,a],[a,2q]]`, with determinant `4q-a^2`.  The frozen RH-false controls
   satisfy every algebraic adjoint law but have determinant `-(r-s)^2`.  This
   proves that independently geometric positivity is the exact discriminator.
2. The arithmetic modular involution already gives
   `Phi_t^sharp=e^t Phi_(-t)` and `Theta^sharp=1-Theta` on the adele-class
   cyclic trace algebra.  Together with the arithmetic Picard/Jacobian
   realization, this reconstructs preregistered level P2 from existing
   mathematics.

Two exact carrier obstructions narrow the architecture.  The coarse arithmetic
Jacobian quotients away its defining scaling flow, and additive group
completion kills its idempotent Abel--Jacobi prime image.  The weight-one
object must therefore be relative/transverse or cyclic and must retain the
scaling groupoid.

The field-level attack found that the previous first arrow was typed
incorrectly.  The `c=3` finite matrices represent the localized Weil operator,
and the pole-free Dirichlet form subtracts the rank-two pole term.  Neither is
the preregistered remainder obtained by subtracting the positive semilocal
Sonin trace.  The assertion that their signs were equivalent had no comparison
theorem and violated the atlas's framework-merger stop rule.

As a separate localized-Weil lane, the finite kill test survives strongly:
cutoff-free Arb interval `LDL^T`
certifies positive blocks through dimension `401`, and the finite-dictionary
diagnostic gaps remain near `9.5e-4` through band `400`.  The
continuum form audit also kills the easy route: its explicit potential is
strongly negative.  What remains is the constrained nonlocal Poincare
inequality, equivalently a uniform frequency-tail Schur/resolvent bound that
uses the Loewner/jump structure rather than absolute-value estimates.

That analytic problem is legitimate and not RH-equivalent at one fixed width,
but proving it would establish a localized Weil theorem, not Sonin domination.
The numerical `M_0` row also requires an explicit transport before it can be
called the semilocal Mellin-zero constraint.

P3-C0a is now proved in `P3_C0A_SONIN_PROJECTION_TRANSPORT.md`.  In common
Mellin coordinates the one-prime amplification is the bounded invertible
multiplier `d_2(s)=1-2^(-1/2-is)`, giving the exact semilocal Sonin projection

`P_S=D P (P D^*D P)^(-1) P D^*`.

This proves trace-class positivity on the published archimedean admissibility
class.  The corrected first unproved P3 arrow is P3-C0b: derive the trace
kernel in the same explicit basis and normalization as the Weil form, calibrate
it archimedeanly, and assemble `E_S=Q_S-T_S`.  Only then is a sign kill test
meaningful.  See `P3_ATTACK_AND_RESET.md`.

No field-level, RH, zero-free, prime-counting, or new positivity theorem is
claimed.
